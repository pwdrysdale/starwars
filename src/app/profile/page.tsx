import {
    useFilmsStore,
    useSpeciesStore,
    useHomeworldsStore,
    useStarshipsStore,
    useVehicleStore,
} from '@/store/additionalResources';
import { Species, Film, Starship, Vehicle, Homeworld } from '@/interfaces';

const Profile = () => {
    return (
        <div>
            <h1>Profile</h1>
        </div>
    );
};

export default Profile;
